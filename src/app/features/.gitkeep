# This file ensures the folder is tracked by Git
# Add feature modules here (products/, cart/, checkout/, auth/, orders/, etc.)
# Each feature should have its own folder with components/, services/, models/, pages/
